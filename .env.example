# Local `vercel dev` only. Never commit real keys.
#
# Direct OpenAI (recommended when you have an OpenAI API key):
OPENAI_API_KEY=
#
# Vercel AI Gateway alternative (Vercel production can also use OIDC without a key):
AI_GATEWAY_API_KEY=

